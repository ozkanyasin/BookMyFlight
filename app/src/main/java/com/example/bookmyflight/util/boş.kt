package com.example.bookmyflight.util


//adapter
/*holder.view.textScheduleTime.text =
            flightList[position].actualLandingTime?.substring(11,16) ?: "Currently Unavailable"*/


/* @RequiresApi(Build.VERSION_CODES.O)
    override fun onCreateOptionsMenu(menu: Menu, inflater: MenuInflater) {
        inflater.inflate(R.menu.filter_menu,menu)
        val item = menu?.findItem(R.id.dateFilter)
        val datePicker = item?.actionView as DatePicker

        datePicker.setOnDateChangedListener(object : DatePicker.OnDateChangedListener{
            override fun onDateChanged(
                view: DatePicker?,
                year: Int,
                monthOfYear: Int,
                dayOfMonth: Int
            ) {
                viewModel.getFlightsByDate("${year.toString()}-${monthOfYear.toString()}-${dayOfMonth.toString()}")

            }

        })


        return super.onCreateOptionsMenu(menu, inflater)
    }*/

        /*
        holder.itemView.setOnClickListener{
            */
        /*mListener.onItemClicked(flightList[position])*//*

        }*/
